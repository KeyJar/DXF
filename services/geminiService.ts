
// AI 功能已被移除，以支持纯静态部署。
// 此文件保留为空壳，防止未清理的引用导致编译错误。

export const analyzeArtifactPhoto = async (base64Image: string, modelId: string = "none"): Promise<any> => {
  console.log("AI 功能已禁用");
  return null;
};

export const analyzeArtifactDrawing = async (base64Image: string, modelId: string = "none"): Promise<any> => {
  console.log("AI 功能已禁用");
  return null;
};
